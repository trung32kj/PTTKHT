# Django
DJANGO_SECRET_KEY=doi-thanh-chuoi-ngau-nhien-dai
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CSRF_TRUSTED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000

# Database
# Cách 1 (production - Neon/Postgres): dùng DATABASE_URL, các biến DB_* bên dưới sẽ bị bỏ qua
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Cách 2 (dev trên máy): MySQL (mặc định) hoặc DB_ENGINE=sqlite
DB_ENGINE=mysql
DB_NAME=pttkht
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Google Gemini (tính năng chat AI)
GEMINI_API_KEY=
